/* eslint-disable no-unused-vars */

function setupGui() { }

/**
 * 
 * @param {CanvasGraph} ctx 
 * @param {Vector[]} points 
 */
function render(cg, points) {
    return;
}

export {
    render,
    setupGui
}
